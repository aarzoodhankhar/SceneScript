services:
  - type: web
    name: scenescript-api
    runtime: python
    buildCommand: cd backend && pip install -r requirements.txt
    startCommand: cd backend && uvicorn main:app --host 0.0.00 --port $PORT
    envVars:
      - key: MLP_MODEL_PATH
        value: scenescript_clip_mlp.pkl
